{{- range $name, $application := .Values.applications }}
  {{- if (default $application.useNamespace true) }}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ default $application.namespace $name }}
  {{- end }}
{{- end }}
