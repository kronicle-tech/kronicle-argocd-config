{{- range $name, $project := .Values.projects }}
  {{- if (default $project.useNamespace true)}}
---
apiVersion: v1
kind: Namespace
metadata:
  name: {{ default $project.namespace $name }}
  {{- end }}
{{- end }}
