{{- range $name, $project := .Values.projects }}
---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: {{ $name }}
  namespace: argocd   # namespace must be "argocd"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  # The project the application belongs to.
  project: {{ $name }}

  # Source of the application manifests
  source:
    repoURL: https://github.com/kronicle-tech/kronicle-argocd-config.git
    targetRevision: "{{ default $project.targetRevision "HEAD" }}"
    path: {{ $name }}
  {{- if $project.chart }}
    chart: {{ $project.chart }}
  {{- end }}

    helm:
  {{- if $project.helmParameters }}
      parameters:
    {{- range $key, $value := $project.helmParameters }}
        - name: "{{ $key }}"
          value: "{{ $value }}"
    {{- end }}
  {{- end }}

      valueFiles:
        - values-prod.yaml

  destination:
    server: https://kubernetes.default.svc
  {{- if (default $project.useNamespace true)}}
    namespace: {{ default $project.namespace $name }}
  {{- end }}

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
{{- end }}
